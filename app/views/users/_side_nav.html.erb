
<%= image_tag current_user.image.url(:thumb) %><br>
<%= link_to 'Total Buckets: ', items_path %> <%= link_to current_user.items.all.count, items_path%><br>
Completed Buckets: <%= Item.where(user_id: current_user.id).where(completed: true).count %>
<% a = Item.where(user_id: current_user.id).where(completed: true).count %>
<% b = current_user.items.all.count %>
<% @a = a.to_f / b.to_f %>
<p>
<div class="progress">
  <div class="progress-bar" role="progressbar" aria-valuenow="1"
  aria-valuemin="0" aria-valuemax='<%current_user.items.all.count%>' style='width: <%= @a * 100 %>%'>
    <%= @a * 100 %>%
  </div>
</div>
</p>

<div class='custom_text'><%= link_to '', new_item_path, class: 'glyphicon glyphicon-plus btn btn-success btn-lg' %> Add Bucket </div>
